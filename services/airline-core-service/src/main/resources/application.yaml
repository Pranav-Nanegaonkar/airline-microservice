spring:
  application:
    name: airline-core-service
  datasource:
    url: jdbc:mysql://localhost:3306/airline_core_service
    username: root
    password: mysql@123

  jpa:
    hibernate:
      ddl-auto: update      # use 'validate' or 'none' in production
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 5006